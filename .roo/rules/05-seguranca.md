# Segurança
- Nunca exponha segredos no navegador
- RLS ativo em TODAS as tabelas
- Validação no lado do servidor para toda lógica sensível
- Nunca confie apenas em verificações client-side
- Gerenciamento de sessão via Supabase Auth em rotas protegidas
- Limite exposição de dados: retorne só o necessário